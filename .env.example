# ── AI 功能（缺这一项时，关系推演 / 传记撰写 / 灵犀一问 全部不可用）──
#
# 本项目通过 Vercel AI SDK 接入模型，所以「用哪家 AI」是一个配置问题：
#   · 不填 AI_DEFAULT_BASE_URL → 直连 Google Gemini，API_KEY 填 Gemini 密钥
#   · 填了                      → 走该 OpenAI 兼容端点，API_KEY 填对应端点的令牌
#
# 也可以不填：使用者在界面「AI 模型配置」里填自己的密钥即可，
# 那样每个人花自己的额度，且优先于这里的配置。
#
# 密钥（按上面选的那条填）
# Gemini 申请：https://aistudio.google.com/apikey
API_KEY=

# 默认 AI 端点。留空 = 直连 Gemini。
# 填了之后用户无需在界面里手填地址，且该地址自动视为可信（它来自本文件，
# 不是请求方能控制的输入）。任何 OpenAI 兼容服务都可以：
#   自建网关   http://192.168.1.10:3000/v1     （new-api / one-api）
#   本地模型   http://127.0.0.1:8080/v1        （llama.cpp / Ollama）
#   聚合服务   https://openrouter.ai/api/v1
# AI_DEFAULT_BASE_URL=

# 默认模型名。不同端点上挂的模型名不一样，写死必然对不上，
# 所以交给部署方决定（界面里也可以临时覆盖）。
# AI_DEFAULT_MODEL=gemini-3.5-flash

# ── 以下均为可选 ──

# 数据库文件路径。不填则用项目根目录下的 genealogy.db；
# Docker 部署时由 docker-compose.yml 注入 /app/data/genealogy.db
# DB_PATH=./data/genealogy.db

# 允许跨域访问的前端来源，逗号分隔。
# 本地开发端口（5021/4173/8888）已默认放行，仅在把前端部署到独立域名时才需要填。
# ALLOWED_ORIGINS=https://zupu.example.com

# 允许使用的第三方 AI 端点（OpenAI 兼容），逗号分隔。
# ⚠️ 留空时，界面里填写的自定义端点一律被拒绝——因为服务端会带着上面的 API_KEY
#    去请求该地址，任何人填一个自己控制的地址就能拿走你的密钥。
# AI_ALLOWED_BASE_URLS=https://openrouter.ai/api/v1

# 宿主机映射端口。8888 被占用时改这里，不要去改 docker-compose.yml
# HOST_PORT=8889

# 构建镜像时使用的 npm 源。中国大陆网络下建议：
# NPM_REGISTRY=https://registry.npmmirror.com
